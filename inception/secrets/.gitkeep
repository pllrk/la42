# This folder holds the password files. They are generated by `make secrets`
# on first run and are ignored by git (see ../.gitignore).
#
#   db_root_password.txt   MariaDB root password
#   db_password.txt        MariaDB password for the WordPress user
#   credentials.txt        WP_ADMIN_PASSWORD and WP_USER_PASSWORD
